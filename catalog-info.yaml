apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: springreview-location
  description: Location to import all springreview catalog components
spec:
  targets:
    - ./.platform/*.yaml
